  <% @cocktail.doses.each do |dose| %>
  <p> Ingredient : <%= dose.ingredient.name %> </p>
  <p> How much?: <%= dose.description %></p>
   <%= link_to dose_path(dose), method: :delete,  data: { confirm: "Delete this dose ?" } do %>
   <i class="fa fa-trash" aria-hidden="true"></i>
   <hr>
   <% end %>
   <% end %>
